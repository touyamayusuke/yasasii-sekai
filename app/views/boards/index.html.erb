<%= link_to "掲示板を新しくを作る", new_board_path, class: "px-6 py-3 rounded bg-indigo-600 text-white text-lg font-semibold block mb-8 w-fit" %>

<h1 class="text-xl font-bold mb-4">掲示板一覧</h1>

<div class="space-y-2">
  <% @boards.each do |b| %>
    <div class="p-3 bg-white border rounded">
      <%= link_to b.title, board_path(b), class: "text-indigo-700 underline" %>
    </div>
  <% end %>
</div>