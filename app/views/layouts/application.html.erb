<!DOCTYPE html>
<html>
  <head>
    <title>優しい世界</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <header class="bg-white">
      <div class="flex items-center justify-between px-4 py-1 sm:px-6 lg:px-8">
        <%= link_to root_path, class: "flex items-center gap-2" do %>
          <%= image_tag "logo.png", alt: "優しい世界", class: "h-20 w-auto" %>
          <span class="text-lg font-semibold whitespace-nowrap hidden sm:inline">優しい世界</span>
        <% end %>

        <nav class="flex flex-col sm:flex-row sm:items-center gap-2 sm:gap-4 text-xs sm:text-sm">
          <% if user_signed_in? %>
            <span class="whitespace-nowrap">こんにちは、<%= current_user.name || current_user.email %></span>
            <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: "underline w-fit" %>
          <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "underline w-fit" %>
            <%= link_to "新規登録", new_user_registration_path, class: "underline w-fit" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="px-4 py-4 sm:px-6 lg:px-8 flex justify-center">
      <div class="w-full max-w-3xl">
        <% flash.each do |key, val| %>
          <div class="mb-4 p-3 rounded bg-emerald-100 text-emerald-900"><%= val %></div>
        <% end %>
        <%= yield %>
      </div>
    </main>

    <footer class="bg-white border-t mt-12">
      <div class="px-4 py-8 sm:px-6 lg:px-8">
        <div class="max-w-3xl mx-auto">
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-8 mb-8">
            <div>
              <h3 class="font-semibold mb-4">法務</h3>
              <ul class="space-y-2 text-sm text-gray-600">
                <li><%= link_to "利用規約", "#", class: "hover:text-gray-900 underline" %></li>
                <li><%= link_to "プライバシーポリシー", "#", class: "hover:text-gray-900 underline" %></li>
              </ul>
            </div>
            <div>
              <h3 class="font-semibold mb-4">フォロー</h3>
              <ul class="space-y-2 text-sm text-gray-600">
                <li><a href="#" target="_blank" rel="noopener" class="hover:text-gray-900 underline">Twitter</a></li>
              </ul>
            </div>
          </div>
          <div class="border-t pt-8 text-center text-sm text-gray-500">
            <p>&copy; 2025 優しい世界. All rights reserved.</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>