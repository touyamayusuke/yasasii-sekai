<!DOCTYPE html>
<html>
  <head>
    <title>優しい世界</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <header class="bg-white border-b">
      <div class="flex items-center justify-between px-4 py-4 sm:px-6 lg:px-8">
        <%= link_to "優しい世界", root_path, class: "text-lg font-semibold whitespace-nowrap" %>

        <nav class="flex flex-col sm:flex-row sm:items-center gap-2 sm:gap-4 text-xs sm:text-sm">
          <% if user_signed_in? %>
            <span class="whitespace-nowrap">こんにちは、<%= current_user.email %></span>
            <%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: "underline w-fit" %>
          <% else %>
            <%= link_to "ログイン", new_user_session_path, class: "underline w-fit" %>
            <%= link_to "新規登録", new_user_registration_path, class: "underline w-fit" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="px-4 py-4 sm:px-6 lg:px-8 flex justify-center">
      <div class="w-full max-w-3xl">
        <% flash.each do |key, val| %>
          <div class="mb-4 p-3 rounded bg-emerald-100 text-emerald-900"><%= val %></div>
        <% end %>
        <%= yield %>
      </div>
    </main>
  </body>
</html>